#include <stdio.h>
#include <stdlib.h>
#include <math.h>


void main() {
	const carro = 110;
	const caminhao = 80; 
	int dist = 100, S, S0, v, t = 60; 
	
	if(carro = caminhao){
		caminhao =+ 10; 
		t = caminhao; 
		S = S0 + v.t; //fórmula de encontro e ultrapaasagem - Movimento Uniforme 
		printf("Estão em encontro de velocidades!");
	} else(carro < caminhao){
		S =-100;
		printf("Já se cruzaram e faltam %d", S);
	}
	
}
